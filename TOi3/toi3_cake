#include<bits/stdc++.h>
using namespace std;

int main(){
	ios::sync_with_stdio(0); cin.tie(0);
	int n;
	double sum=0.0;	
	cin >> n;
	for(int i=1;i<=n;i++){
		int a,b,c,d,e;
		cin >> a >> b >> c >> d >> e;
		sum+=(a*1)+(b*0.75)+(c*0.5)+(d*0.25)+(e*0.125);
	}
	cout << ceil(sum);
}